print('Программа для знакомств')
#   Создаём список парней.
boys = []
#   Вводим имена, присваивая имя переменной.
add_boys = input('Введите имя парней через запятую и затем "Enter" для завершения ввода: ')
#   Введённые имена разбиваем на список при помощи .split
boys_name = add_boys.split(',')
#   Затем добавляем их в список при помощи .append
boys.append(add_boys)

#   Тут всё то же самое, только с девочками
girls = []
add_girls= input('Введите имя девушек через запятую и затем "Enter" для завершения ввода: ')
girls_name = add_girls.split(',')
girls.append(add_girls)

# Сортируем списки ребят:
new_boys = sorted(boys_name, key=str.lower)
new_girls = sorted(girls_name, key=str.lower)

#   Это я закоментил списки по ДЗ
# boys_name = ['Peter', 'Alex', 'John', 'Arthur', 'Richard']
# girls_name = ['Kate', 'Liza', 'Kira', 'Emma', 'Trisha']

#   Сшиваем отсортированные списки, запаковывая списки в списки - потому что по умолчания zip() выводит в кортеж.
#   Пробовал все
# couple = list(list(i) for i in zip(boys_name, girls_name))

# couple = []
# for i in range(len(boys_name, girls_name)):
#     couple.append([boys_name[i], girls_name[i]])

# couple = []
# for i in zip(boys_name, girls_name):
#     couple.append(list(i))

#   Здесь с использованием функции map() - очень классная штука.
couple = (list(map(list,zip(new_boys, new_girls))))
#   Распаковываем списки из списков
couple_second = '\n'.join([str(i) for i in couple])
#   Тут и так всё понятно
while True:
    if len(new_boys) == len(new_girls):
        print(f'Идеальные пары:\n {couple_second}')
    else:
        print('Количество парней и девушек разняться, кто-то останется без пары!')
        print(f'Идеальные пары:\n {couple_second}')
    break
# ПыСЫ: очень надеюсь, что за комментарии к коду получу хорошие баллы в рейтинге.
